{
  "scripts": {
    "api:install": "cd kanban/kanban-api && npm install .",
    "api:build": "cd kanban/kanban-api && npm run build:bash",
    "api:start": "cd kanban/kanban-api && npm run dev",
    "api:deploy": "cd kanban/kanban-api && npm run deploy",
    "api:deploy:bash": "cd kanban/kanban-api && npm run deploy:bash",
    "deploy": "npm run api:deploy:bash && npm run frontend:deploy"
  }
}
